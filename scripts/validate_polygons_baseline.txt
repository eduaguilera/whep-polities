# Polities that declare a polygon_status asserting a polygon exists, but whose
# polygon_feature_id the build cannot resolve, so nothing is attached.
#
# This is a TRACKED BACKLOG, not an exemption. Each needs either a real builder
# in scripts/sources/constructed/build.py (most are composed unions/differences
# described in prose, which nothing can resolve) or an honest downgrade to
# polygon_status: unassigned with the reason recorded on the page.
#
# Baselined 2026-07-24 so scripts/validate_polygons.py fails on NEW occurrences
# rather than staying permanently red. Remove a line as you fix it. The check is
# bidirectional: a line left here after the row is fixed FAILS, so the list cannot
# drift into a record of history.
#
# Each entry is tagged with what actually blocks it, because the three categories
# need completely different work (diagnosed 2026-07-29):
#
#   [compose]  the feature id describes a union, difference or complement of several
#              source features. Nothing can resolve it as a single id; it needs a
#              builder in scripts/sources/constructed/build.py. 5 rows (was
#              11). Built so far: BTL-1920-1957, TTPI-1947-1994,
#              AEF-1910-1960, GBM-1895-1946, SYL-1944-1953. GCT-1919-1956 LEFT this list on
#              2026-08-06: its recipe was registered once the row was retyped `aggregate`, which
#              is what issue 47's blocker actually was. Its
#              recipe is verified but unregistered — see issue 47.
#              The 5 that remain all need GADM extracts absent locally
#              (Canada, India, Indonesia).
#   [no-id]    there is no feature id at all (`None` / `NA`). A source and an id must
#              be identified first, or the row downgraded to `unassigned` with the
#              reason recorded. 4 rows (was 6; NRH-1911-1953 and GRC-1830-1881
#              turned out to have faithful polygons that simply were not bound).
#   [extract]  the id IS a single valid identifier and the slug is now correct, but
#              the GADM copy in data/geodata/ is a curated subset (82 adm0 and 620
#              adm1 features, small territories only) that does not contain the
#              country. Needs a fuller GADM download, nothing else. 3 rows.
#
# 13 rows remain, from 20.
# CAN-1800-1866's declared recipe would OVERSTATE BY 2.26x and must not be built as
#   written. gadm ON+QC+NB+NS+PE measures 2,735,024 km2 against the page's own declared
#   1,209,852. Modern Ontario and Quebec absorbed Rupert's Land, Keewatin and Ungava
#   between 1870 and 1912; the Province of Canada was the settled southern strip. The
#   page's own component list says 'Ontario + Quebec ~1,076,000 km2' where modern ON+QC
#   is 2,600,836. Declared area right, declared recipe wrong. Checked 2026-08-07.
CAN-1800-1866   # [compose] proxy: see the note above, the stated recipe is 2.26x too large
FRIN-1816-1954   # [compose] estimate: composed-union-puducherry-karikal-mahe-yanam-chandernagore
# IDN-BLB-1949-1951's declared recipe is WRONG and must not be built as written:
#   IDN.1_1 is ACEH, on Sumatra, ~1,700 km from Bali. IDN.20_1 is Nusa Tenggara Barat,
#   which is Lombok PLUS SUMBAWA at 19,670 km2. Bali 5,591 + NTB 19,670 = 25,261 against
#   the page's declared 10,505 -- and 10,505 is Bali + Lombok ALONE, so the declared AREA
#   is right. Separating Lombok from Sumbawa needs GADM adm2, which this repo does not
#   build (only adm0 and adm1). Blocked on data, not on a decision. Checked 2026-08-07.
IDN-BLB-1949-1951   # [compose] proxy: Bali + Lombok; see the note above, adm2 required
# IDN-OTH-1949-1951 is the complement of JVM and BLB, so it is blocked on BLB.
IDN-OTH-1949-1951   # [compose] proxy: IDN-complement-JVM-BLB; blocked on IDN-BLB

