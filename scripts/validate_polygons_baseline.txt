# Polities that declare a polygon_status asserting a polygon exists, but whose
# polygon_feature_id the build cannot resolve, so nothing is attached.
#
# THE LIST IS EMPTY, AND THAT IS THE CURRENT STATE, NOT AN OVERSIGHT.
#
# Measured 2026-08-13 on a fresh `python3 scripts/build_database.py` with
# data/geodata/ present (767 pages scanned, 735 geometries attached):
#
#   check C: 32 polities have no geometry; 0 of them declare a polygon_status
#            that asserts one (0 baselined, 0 new, 0 stale)
#
#   of those 32, 31 are `polygon_status: unassigned` and the 32nd is the retired
#   duplicate page DJI-1886-2025, which declares no polygon status at all. None of
#   the 32 is reachable through data/final/faostat_area_polity_map.csv, so no
#   published row routes to a polity that promises a polygon and has none.
#
# All 20 rows tracked here for issue #3 are resolved: 17 got real geometry (11 via
# builders in scripts/sources/constructed/build.py — AEF, BTL, FRIN, GBM, GCT,
# IDN-BLB, IDN-JVM, IDN-OTH, PTIND, SYL, TTPI; 6 by naming or correcting a single
# source feature id that already existed — ALK, GRC, NRH, RYU-1937-1945,
# RYU-1945-1972, SER) and 3 were withdrawn to
# `polygon_status: unassigned` with the blocker recorded on the page
# (CAN-1800-1866, CAN-1866-1870, TRS-1947-1954).
#
# KEEP THIS FILE. scripts/validate_polygons.py and scripts/write_manifest.py both
# read it; an empty file means "no polity is allowed to claim a polygon it lacks",
# which is exactly the invariant wanted now that the backlog is cleared. Deleting
# it would work today but would make it invisible that the ratchet exists.
#
# HOW TO USE IT AGAIN. If a new page legitimately has to claim a polygon it cannot
# yet carry, add one line with just the polity_code and a `#` comment saying what
# blocks it. The check is bidirectional: a line left here after the row is fixed
# FAILS, so the list cannot drift into a record of history. Prefer fixing or
# downgrading the row over adding a line.
#
# Do not re-add prose diagnoses here. The three blocker categories this file used
# to carry ([compose] needs a builder, [no-id] has no feature id, [extract] needs a
# fuller GADM download) all turned out to be resolvable, and the per-row reasoning
# now lives where it belongs: in the builder docstrings in
# scripts/sources/constructed/build.py and in the polygon sections of the pages
# themselves. wiki/log.md keeps the chronology.
