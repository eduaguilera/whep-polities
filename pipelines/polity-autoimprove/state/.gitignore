# Derived per-run artifacts (regenerated by 00_/01_/02_; never committed)
findings.json
matched_rows.parquet
territorial_flagged.json
run_report.md
assertions.json
assertions_*.json
# regenerated by prepare_federico_tena.py from the tracked xlsx (issue 26)
federico_tena_intake.csv
verdicts_pending*.json
structural_snapshot.json
# Persistent state below is committed: review_ledger.csv, applied_aliases.csv
match_confidence.csv
audit_sample.csv
# regenerable, deterministic (review_stats.py --audit-sample): a BLANK human-audit
# template. If someone actually scores one by hand, commit it under a new name
# (audit_scored_<date>.csv) — that is a measurement, not a derived artifact.
audit_sample_banked.csv
